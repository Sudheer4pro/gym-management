rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Multi-tenant gym management access rules
    // Allow read and write for authenticated users and application services
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
